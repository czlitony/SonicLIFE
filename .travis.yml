language: node_js
node_js:
  - "5.4"

install:
  - npm install

services:
  - mongodb
  - npm start

before_script:
  - sleep 15
  - mongo sonicwall --eval 'db.user.remove();'
  - sleep 5

script:
  - npm test

after_script:
  - killall npm
